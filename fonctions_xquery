xquery version "3.0";

declare function local:getListeDepartement()  {
    
    for $x in distinct-values(doc("merimee-MH.xml")/csv_data/row/DPT)
    order by $x
    return <DPT>{data($x)}</DPT>
};

local:getListeDepartement()
